import 'package:flutter/material.dart';

class LessonPlan extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Lesson Plan Title")),
      body: Container(
        child: Text("sample"),
      ),
    );
  }
}